# Web plugin

Tool: `run_web_agent`.

- Use this when the user needs something searched, fetched, opened, navigated, submitted, or otherwise completed on a website.
- Prefer direct navigation when the target URL/site is already known; don't browse unnecessarily.
- Streams a live view to the UI while it works - you don't need to narrate every intermediate step yourself.
